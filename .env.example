# Database Configuration for Docker Compose
DB_VERSION=16
DB_PORT=5432
DB_NAME=demo
DB_USER=postgres
DB_PASSWORD=postgres

# Prisma Database URL
# NOTE: .env files don't support variable interpolation, so use actual values here
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DB_URL="postgresql://postgres:postgres@localhost:5432/demo?schema=public"

# JWT Configuration
JWT_SECRET=jwvGQbaicaICJXdZTI74pntcZp+8D7B0FJo7OS04p78=
